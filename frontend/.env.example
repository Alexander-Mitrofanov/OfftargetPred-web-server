# Public HTTPS API origin. This is configuration, not a secret.
# Leave empty only for local development through the Vite /api proxy.
VITE_API_URL=https://your-public-backend.example.org
# GitHub repository Pages path; use / for a custom domain.
BASE_PATH=/OfftargetPred-web-server/
